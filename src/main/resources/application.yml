server:
  port: 8081

spring:
  application:
    name: cocktail-list
  
  datasource:
    url: jdbc:mysql://localhost:3306/cocktails
    username: cocktail_user
    password: cocktail_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2MB

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  
  security:
    oauth2:
      resourceserver:
        jwt:
          # Usa solo jwk-set-uri senza issuer-uri per accettare token da localhost E keycloak
          jwk-set-uri: ${KEYCLOAK_AUTH_SERVER_URL:http://localhost:8080}/realms/cocktail_realm/protocol/openid-connect/certs

upload:
  path:  uploads/cocktails


keycloak:
  realm: cocktail_realm
  auth-server-url: ${KEYCLOAK_AUTH_SERVER_URL:http://localhost:8080}
  resource: cocktail-client
  public-client: true
  admin:
    username: admin
    password: admin

# Springdoc OpenAPI / Swagger UI
springdoc:
  swagger-ui:
    oauth:
      client-id: cocktail-client
      use-pkce-with-authorization-code-grant: false
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
  
logging:
  level:
    org.springframework.security: DEBUG
    org.keycloak: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://mysql:3306/cocktails
    username: cocktail_user
    password: cocktail_pass
  security:
    oauth2:
      resourceserver:
        jwt:
          # Usa solo jwk-set-uri per validare la firma, accetta token da localhost (browser) e keycloak (Docker)
          jwk-set-uri: http://keycloak:8080/realms/cocktail_realm/protocol/openid-connect/certs

upload:
  path: /app/uploads/cocktails


keycloak:
  auth-server-url: http://keycloak:8080
